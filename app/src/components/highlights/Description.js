import React from 'react'
import Fade from 'react-reveal/Fade';

const Description = () => {
    return (
        <Fade>
            <div className="center_wrapper">
                <h2>Highlights</h2>
                <div className="highlight_description">
                On the next section we will install the dependencies we need for this project. If you are using NPM copy the "npm" line or the yarn line if you use "yarn".
                </div>
            </div>
        </Fade>
    )
}

export default Description
